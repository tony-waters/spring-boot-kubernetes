apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.names.app }}
  namespace: {{ .Values.namespaces.app }}
spec:
  type: ClusterIP
  selector:
    app: {{ .Values.names.app }}
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80